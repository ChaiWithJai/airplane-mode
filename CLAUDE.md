# CLAUDE.md

This project is built by a harnessed loop. **Read `AGENTS.md`** — it is the operating manual (the loop, the hard rules, the gates, the HITL checkpoints). Everything in it applies to Claude Code verbatim.

Quick orientation: `README.md` → `CANON.md` → `docs/superpowers/specs/2026-06-25-portable-core-architecture-design.md` → `files/rfc-002-final-ship.md` → `AGENTS.md` → `backlog/README.md`.

One task per iteration. Run `./run.sh gates` before committing. Never disable a gate. Stop and ask at milestone boundaries and before any `gh`/remote/publish action.
