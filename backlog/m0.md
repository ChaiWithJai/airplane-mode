# M0 — Content: the `coach-session` pack + golden notes
*Goal: the truth set everything else is measured against exists. The gating dependency — nothing downstream is testable without it.*
*HITL: Jai reviews the golden notes + schema before M1 opens.*

This is "earn the map" (Constitution III): hand-derive the identifier taxonomy and note schema by hand-scrubbing real-shaped (synthetic) notes. That manual pass IS the spec for the rules executor and the structurer.

## Tasks

- [x] **M0-T01** ✅ — Create `packs/coach-session/` skeleton: `pack.yaml`, `recognizers/`, `schema.yaml`, `policy.yaml`, `sink.yaml`, `eval/`. Use the concrete YAML/JSON from `files/rfc-002-final-ship.md` §4 (YAML for config, JSON for recognizers — **not HCL**; D2 resolved). · *done-when:* `pack.yaml` parses; layout matches RFC-002.
- [x] **M0-T02** ✅ — Author `recognizers/members.json` + `recognizers/people.json` (Presidio-style: regex + context + checksum + deny/allow lists) covering the Safe Harbor 18 baseline + coach-specific IDs. · *done-when:* each recognizer is valid JSON with `supported_entity`, `patterns`, `context`.
- [x] **M0-T03** ✅ — Author `schema.yaml` for the coach-session record (`client_pseudonym, themes[], commitments[], follow_ups[], risk_flags[], next_touch`). · *done-when:* schema validates; matches RFC-002 §4.
- [x] **M0-T04** ✅ — Author `policy.yaml` (Safe-Harbor profile, `recallThreshold: 0.99`, `onResidual: block`, the autonomy reward block + `forbiddenTerms`). · *done-when:* present; reward block lists autonomy signals and forbidden engagement terms.
- [x] **M0-T05** ✅ (20 notes, 70 redactions/54 hard, 3 clean, 0 substring mismatches) — Write **~20 golden notes**: `eval/golden/note-NNN.txt` (synthetic, fake people) + `eval/expected/note-NNN.json` (expected redactions). Include **≥1 hard case** per the demo spec (a name buried in prose; a date in words; a disclosed relationship). · *done-when:* 20 pairs exist; every expected redaction is hand-labeled; the hard case is marked.
- [x] **M0-T06** ✅ — Write `sink.yaml` (Slack, channel map, credential **sourced** from keychain — never stored). · *done-when:* present; `receives: deidentified-record-only`.

## Done-when (milestone)
Twenty hand-labeled golden pairs, a valid five-file pack, and a marked hard case. No code runs yet — this is the spec. → **HITL: Jai review.**
