# M2 — The full loop on desktop (stub — expand at M1's HITL)
*Goal: `./run.sh demo` runs end-to-end: text in → clean structured record → Slack → one client-paced follow-up note. Depends on: M1 ✅.*

Outline (flesh out when M1 lands — don't draw the owl now):
- [ ] **M2-T01** — Structurer: classify note archetype, then extract to the pack schema (plan-then-execute). Schema validation is deterministic, not AI.
- [ ] **M2-T02** — Follow-up generator (heuristic v1): a client-paced nudge from the client's own commitment; logs the **autonomy delta**, never engagement; surfaces escalation on clinical-risk signal.
- [ ] **M2-T03** — Real Slack `Sink` (Block Kit), bot token sourced from keychain; offline queue (block while offline, flush when online).
- [ ] **M2-T04** — Turn on the two ★ gates: `reward-lint` (no engagement terms) and `scope-boundary` (escalation path present).

**Done-when:** the loop runs to a real Slack channel, the two ethical gates are live, no identifier leaks. → HITL.
