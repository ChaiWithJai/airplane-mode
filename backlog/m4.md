# M4 — Agent surface + belief for non-builders (stub — expand at M3's HITL)
*Goal: the MCP/agent shell (agent-native proof) + the public README + a recorded demo. Depends on: M2 ✅ (needs the full loop) — MCP is a thin transport over the same core API.*

- [ ] **M4-T01** — MCP server shell: expose `scrub` and `demo` as agent-callable tools over the **same `airplane-core` API the CLI uses**; `Sink` returns the clean record as the tool result. Keep it gate-clean (default-deny applies to tool egress too).
- [ ] **M4-T02** — Prove MCP scrub == CLI scrub on the golden notes (one core, two transports).
- [ ] **M4-T03** — Finalize the public `README.md` (champion-facing): the bet, the 60-second story, the five-file pack reveal, "make it yours / repatriate it," the honest caveats. Echo PrismML positioning per `docs/seed/bonsai-ecosystem-brief.md` §B.
- [ ] **M4-T04** — Record the demo (Tier-0 belief for non-builders); link from README.

**Done-when:** an agent can run the scrub, a developer can reproduce it, a non-builder can watch it. → HITL (this is the "ready for public repo" boundary).
