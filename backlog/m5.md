# M5 — The environment grows (gated stub — expand only on real demand)
*Goal: the trajectory recorder + counter. We GROW the environment; we train NO policy (ADR-013). Depends on: M2 ✅.*

- [ ] **M5-T01** — Trajectory recorder: deposit one de-identified `(s, a, r, s')` per completed loop, **through the same verifier gate** (gate guards environment ingress exactly as it guards Slack egress).
- [ ] **M5-T02** — The counter increments on each gate-clean trajectory; surface it in the demo.
- [ ] **M5-T03** — Trajectory store is enclave-encrypted at rest (Gap 3); reward logs the autonomy delta only.

**Hard line:** no policy training, no pooling. The reward-lint gate still applies. The three prose-only open questions (autonomy-delta measurement, re-identification floor, "is the follow-up good?") stay prose — do not pretend they're enforced. → HITL.
